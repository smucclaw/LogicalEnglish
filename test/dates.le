the target language is: prolog.

the templates are:
  *a date* is before *an other date*.
  p of *a date*.

the knowledge base dates includes:
  a date is before an other date
  if date is a n days before other date
  and n > 0.

  p of a date
  if date is 3 months before today.

query q0 is:
  which date is before 2023-2-10.

query q1 is:
  p of which date.

query q2 is:
  today is which date.

query q3 is:
  yesterday is which date.  

query q4 is:
  which date is today.

query q5 is:
  yesterday was which date.